/// Model representing a bundled test puzzle.
class TestPuzzle {
  const TestPuzzle({required this.name, required this.assetPath});

  /// Display name for the puzzle.
  final String name;

  /// Asset path to the puzzle image.
  final String assetPath;
}
